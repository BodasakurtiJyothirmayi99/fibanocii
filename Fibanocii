#include<stdio.h>
int main()
{
int n,next,t1=0,t2=1;
printf("enter the series limit");
scnaf("%d",&n);
for (i = 1; i <= n; ++i)
    {
        printf("%d, ", t1);
        nextTerm = t1 + t2;
        t1 = t2;
        t2 = nextTerm;
    }
return 0;
}
